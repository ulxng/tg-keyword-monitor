services:
  monitor:
    build: .
    working_dir: /app
    command: python main.py
    restart: unless-stopped
    stdin_open: true
    tty: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/app
